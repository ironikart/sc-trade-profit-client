const matrix = require(__dirname + '/../../data/ship_matrix_parsed.json');
const systems = require(__dirname + '/../../data/systems_parsed.json');
const points = require(__dirname + '/../../data/points_parsed.json');
const pointsGraph = require(__dirname + '/../../data/systems_graph.json');

export {
    matrix,
    systems,
    points,
    pointsGraph
};